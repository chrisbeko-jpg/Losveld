'use client';
import './account.css';
import Link from 'next/link';
import { useEffect, useState } from 'react';
import { useRouter, useSearchParams } from 'next/navigation';
import { supabase } from '../../lib/supabase';
import { Dog, Star, ShieldCheck, BadgeCheck, AlertTriangle, Heart, CalendarDays, MapPin } from 'lucide-react';

export default function Account(){
 const router=useRouter(); const params=useSearchParams(); const [user,setUser]=useState(null); const [profile,setProfile]=useState(null); const [dogs,setDogs]=useState([]); const [bookings,setBookings]=useState([]); const [name,setName]=useState(''); const [breed,setBreed]=useState(''); const [sex,setSex]=useState('reu'); const [busy,setBusy]=useState(true);
 async function load(){const {data:{user}}=await supabase.auth.getUser(); if(!user){router.push('/login');return;} setUser(user); const [{data:p},{data:d},{data:b}]=await Promise.all([supabase.from('profiles').select('*').eq('id',user.id).single(),supabase.from('dogs').select('*').eq('owner_id',user.id).order('created_at'),supabase.from('bookings').select('*,spots(name,city,price_per_hour)').eq('guest_id',user.id).order('starts_at',{ascending:true})]); setProfile(p); setDogs(d||[]);setBookings(b||[]);setBusy(false);}
 useEffect(()=>{load()},[]);
 async function addDog(e){e.preventDefault(); if(!name)return; const {error}=await supabase.from('dogs').insert({owner_id:user.id,name,breed,sex}); if(!error){setName('');setBreed('');load();}}
 async function cancel(id){if(!confirm('Deze reservering annuleren?'))return;const {error}=await supabase.from('bookings').update({status:'cancelled'}).eq('id',id).eq('guest_id',user.id);if(!error)load()}
 async function logout(){await supabase.auth.signOut();router.push('/');}
 if(busy)return <main><div className="wrap" style={{padding:'60px 0'}}>Mijn LOS! laden...</div></main>;
 const upcoming=bookings.filter(b=>new Date(b.starts_at)>new Date()&&b.status!=='cancelled'); const past=bookings.filter(b=>new Date(b.starts_at)<=new Date()||b.status==='cancelled');
 return <main><header className="nav wrap"><Link className="logo" href="/">LOS<span>!</span></Link><nav><Link href="/zoeken">Vind een LOS!</Link><Link href="/host">Verhuur je veld</Link><button className="ghost" onClick={logout}>Uitloggen</button></nav></header><section className="accountHero"><div className="wrap accountHeroGrid"><div><span className="kicker">MIJN LOS!</span><h1>Hoi {profile?.display_name||user?.email?.split('@')[0]}</h1><p>Beheer je honden, boekingen en reputatie op één plek.</p></div><div className="trustScore"><ShieldCheck/><div><small>LOS! Trustscore</small><strong>{Number(profile?.trust_score||5).toFixed(1).replace('.',',')}</strong><span>{profile?.account_status==='active'?'Account actief':'Accountstatus controleren'}</span></div></div></div></section><section className="wrap accountGrid"><div>{params.get('booked')==='1'&&<div className="promise" style={{marginBottom:20}}><ShieldCheck/><div><strong>Je LOS! is gereserveerd.</strong><span>Het tijdslot is direct geblokkeerd voor andere hondeneigenaren.</span></div></div>}<h2 className="accountTitle">Komende boekingen</h2>{upcoming.map(b=><div className="reviewCard" key={b.id} style={{marginBottom:12}}><div><strong>{b.spots?.name||'LOS!'}</strong><span className="privatePill"><ShieldCheck size={12}/> PRIVÉ</span></div><p><MapPin size={13}/> {b.spots?.city} &nbsp; · &nbsp; <CalendarDays size={13}/> {new Date(b.starts_at).toLocaleString('nl-NL',{dateStyle:'medium',timeStyle:'short'})}</p><div className="reviewTraits"><span>{b.dog_count} hond{b.dog_count>1?'en':''}</span><span>€{Number(b.total_amount).toFixed(2).replace('.',',')}</span><span>{b.status}</span></div><button className="secondary" style={{marginTop:12}} onClick={()=>cancel(b.id)}>Annuleer boeking</button></div>)}{upcoming.length===0&&<div className="reviewCard"><strong>Nog niets gepland.</strong><p>Zoek een privéveld en boek een vrij tijdslot.</p><Link href="/zoeken">Vind een LOS!</Link></div>}<h2 className="accountTitle">Mijn honden</h2>{dogs.map(d=><div className="profileCard" key={d.id}><div className="dogAvatar"><Dog/></div><div><span className="verified"><BadgeCheck size={15}/> Gekoppeld aan jouw account</span><h2>{d.name}</h2><p>{d.breed||'Ras niet ingevuld'} · {d.sex||'Onbekend'}</p></div></div>)}<h2 className="accountTitle">Hond toevoegen</h2><form className="dogForm" onSubmit={addDog}><label>Naam<input value={name} onChange={e=>setName(e.target.value)} required/></label><label>Ras<input value={breed} onChange={e=>setBreed(e.target.value)} placeholder="Bijv. Labrador"/></label><label>Geslacht<select value={sex} onChange={e=>setSex(e.target.value)}><option value="reu">Reu</option><option value="teef">Teef</option></select></label><button className="bookNow">Hond toevoegen</button></form>{past.length>0&&<><h2 className="accountTitle">Eerdere / geannuleerde boekingen</h2>{past.slice(-5).reverse().map(b=><div className="reviewCard" key={b.id} style={{marginBottom:10}}><strong>{b.spots?.name||'LOS!'}</strong><p>{new Date(b.starts_at).toLocaleString('nl-NL',{dateStyle:'medium',timeStyle:'short'})} · {b.status}</p></div>)}</>}</div><aside><div className="sideCard"><h3>Jouw reputatie</h3><div className="scoreBig"><Star fill="currentColor"/> {Number(profile?.trust_score||5).toFixed(1).replace('.',',')}</div><p>Je reputatie groeit automatisch op basis van beoordelingen na echte boekingen.</p><div className="safeStatus"><ShieldCheck/> {profile?.account_status==='active'?'Geen actieve beperkingen':'Accountreview actief'}</div></div><div className="sideCard"><h3>Hoe vertrouwen werkt</h3><p>Hosts en hondeneigenaren beoordelen elkaar na een boeking. Incidentmeldingen worden apart beoordeeld en kunnen leiden tot waarschuwingen of beperkingen.</p><div className="incidentInfo"><AlertTriangle/><span>Één slechte review veroorzaakt dus niet automatisch een ban.</span></div></div><div className="sideCard"><Heart/><h3>Favorieten</h3><p>Favorieten koppelen we later aan je account.</p></div></aside></section></main>}

'use client';
import './account.css';
import Link from 'next/link';
import {useEffect,useState} from 'react';
import {useRouter} from 'next/navigation';
import {supabase} from '../../lib/supabase';
import {Dog,Star,ShieldCheck,BadgeCheck,AlertTriangle,CalendarDays,MapPin,Wallet,Fence,Users,Plus,BarChart3,Settings} from 'lucide-react';

export default function Account(){
 const router=useRouter();const [view,setView]=useState('renter'),[booked,setBooked]=useState(false),[busy,setBusy]=useState(true),[user,setUser]=useState(null),[profile,setProfile]=useState(null),[dogs,setDogs]=useState([]),[bookings,setBookings]=useState([]),[spots,setSpots]=useState([]),[hostBookings,setHostBookings]=useState([]),[availability,setAvailability]=useState([]),[name,setName]=useState(''),[breed,setBreed]=useState(''),[sex,setSex]=useState('reu');
 async function load(){const {data:{user:u}}=await supabase.auth.getUser();if(!u){router.push('/login');return}setUser(u);const [{data:p},{data:d},{data:b},{data:s}]=await Promise.all([supabase.from('profiles').select('*').eq('id',u.id).single(),supabase.from('dogs').select('*').eq('owner_id',u.id).order('created_at'),supabase.from('bookings').select('*,spots(name,city,slug)').eq('guest_id',u.id).order('starts_at',{ascending:true}),supabase.from('spots').select('*').eq('host_id',u.id).order('created_at',{ascending:false})]);setProfile(p);setDogs(d||[]);setBookings(b||[]);setSpots(s||[]);if((s||[]).length){const ids=s.map(x=>x.id);const [{data:hb},{data:a}]=await Promise.all([supabase.from('bookings').select('*').in('spot_id',ids).order('starts_at',{ascending:true}),supabase.from('availability').select('*').in('spot_id',ids)]);setHostBookings(hb||[]);setAvailability(a||[])}setBusy(false)}
 useEffect(()=>{const q=new URLSearchParams(window.location.search);if(q.get('view')==='host')setView('host');if(q.get('booked')==='1')setBooked(true);load()},[]);
 async function addDog(e){e.preventDefault();if(!user||!name)return;const {error}=await supabase.from('dogs').insert({owner_id:user.id,name,breed,sex});if(!error){setName('');setBreed('');load()}}
 async function logout(){await supabase.auth.signOut();router.push('/')}
 if(busy)return <main><div className="wrap" style={{padding:'60px 0'}}>Mijn LOS! laden...</div></main>;
 const now=new Date(),upcoming=bookings.filter(b=>new Date(b.starts_at)>now&&b.status!=='cancelled'),hostUpcoming=hostBookings.filter(b=>new Date(b.starts_at)>now&&b.status!=='cancelled'),completed=hostBookings.filter(b=>new Date(b.ends_at)<=now&&b.status!=='cancelled'),gross=completed.reduce((n,b)=>n+Number(b.total_amount||0),0),fee=gross*.15,balance=gross-fee;
 return <main><header className="nav wrap"><Link className="logo" href="/">LOS<span>!</span></Link><nav><Link href="/zoeken">Vind een LOS!</Link><Link href="/host">Verhuur je veld</Link><button className="ghost" onClick={logout}>Uitloggen</button></nav></header><section className="accountHero"><div className="wrap accountHeroGrid"><div><span className="kicker">MIJN LOS!</span><h1>Hoi {profile?.display_name||user?.email?.split('@')[0]}</h1><p>{view==='host'?'Beheer je velden, bezoekers en opbrengsten.':'Beheer je honden, boekingen en reputatie.'}</p></div><div className="trustScore"><ShieldCheck/><div><small>LOS! Trustscore</small><strong>{Number(profile?.trust_score||5).toFixed(1).replace('.',',')}</strong><span>Account {profile?.account_status||'actief'}</span></div></div></div></section><div className="wrap accountSwitch"><button className={view==='renter'?'active':''} onClick={()=>setView('renter')}><Dog size={17}/> Ik boek LOS!</button><button className={view==='host'?'active':''} onClick={()=>setView('host')}><Fence size={17}/> Ik verhuur</button></div>{view==='renter'?<section className="wrap accountGrid"><div>{booked&&<div className="promise" style={{marginBottom:20}}><ShieldCheck/><div><strong>Je LOS! is gereserveerd.</strong><span>Het gekozen tijdslot is nu geblokkeerd voor anderen.</span></div></div>}<h2 className="accountTitle">Komende boekingen</h2>{upcoming.length?upcoming.map(b=><div className="reviewCard" key={b.id} style={{marginBottom:12}}><strong>{b.spots?.name||'LOS!'}</strong><p><MapPin size={13}/> {b.spots?.city} · <CalendarDays size={13}/> {new Date(b.starts_at).toLocaleString('nl-NL',{dateStyle:'medium',timeStyle:'short'})}</p><div className="reviewTraits"><span>{b.dog_count} hond{b.dog_count>1?'en':''}</span><span>€{Number(b.total_amount).toFixed(2).replace('.',',')}</span></div>{b.spots?.slug&&<Link href={`/veld/${b.spots.slug}`}>Bekijk veld</Link>}</div>):<div className="reviewCard"><strong>Nog niets gepland.</strong><p>Zoek een privéveld en reserveer een vrij tijdslot.</p><Link href="/zoeken">Vind een LOS!</Link></div>}<h2 className="accountTitle">Mijn honden</h2>{dogs.map(d=><div className="profileCard" key={d.id}><div className="dogAvatar"><Dog/></div><div><span className="verified"><BadgeCheck size={15}/> Gekoppeld aan jouw account</span><h2>{d.name}</h2><p>{d.breed||'Ras niet ingevuld'} · {d.sex||'Onbekend'}</p></div></div>)}<h2 className="accountTitle">Hond toevoegen</h2><form className="dogForm" onSubmit={addDog}><label>Naam<input value={name} onChange={e=>setName(e.target.value)} required/></label><label>Ras<input value={breed} onChange={e=>setBreed(e.target.value)}/></label><label>Geslacht<select value={sex} onChange={e=>setSex(e.target.value)}><option value="reu">Reu</option><option value="teef">Teef</option></select></label><button className="bookNow">Hond toevoegen</button></form></div><aside><div className="sideCard"><h3>Jouw reputatie</h3><div className="scoreBig"><Star fill="currentColor"/> {Number(profile?.trust_score||5).toFixed(1).replace('.',',')}</div><p>Hosts en hondeneigenaren beoordelen elkaar na echte boekingen.</p><div className="incidentInfo"><AlertTriangle/><span>Een incident wordt apart beoordeeld; één slechte review betekent niet automatisch een ban.</span></div></div></aside></section>:<section className="wrap hostDashboard"><div className="hostStats"><div><Wallet/><span>Beschikbaar saldo</span><strong>€{balance.toFixed(2).replace('.',',')}</strong><small>na 15% LOS!-fee</small></div><div><CalendarDays/><span>Aankomende boekingen</span><strong>{hostUpcoming.length}</strong><small>over al je velden</small></div><div><Fence/><span>Actieve velden</span><strong>{spots.filter(s=>s.status==='published').length}</strong><small>{spots.length} totaal</small></div><div><BarChart3/><span>Bruto omzet</span><strong>€{gross.toFixed(2).replace('.',',')}</strong><small>voltooide boekingen</small></div></div><div className="hostDashGrid"><div><div className="dashTitle"><h2>Mijn velden</h2><Link href="/host" className="primary"><Plus size={16}/> Nieuw veld</Link></div>{spots.length?spots.map(s=><div className="hostSpotCard" key={s.id}><div><span className="privatePill"><ShieldCheck size={12}/> {s.status==='published'?'LIVE':'GEPAUZEERD'}</span><h3>{s.name}</h3><p><MapPin size={14}/> {s.city} · {Number(s.size_m2||0).toLocaleString('nl-NL')} m²</p><div className="reviewTraits"><span>{s.fence_type||'Omheining onbekend'}</span><span>€{Number(s.price_per_hour).toFixed(2).replace('.',',')} / uur</span></div><div style={{display:'flex',gap:8,marginTop:12}}><Link href={`/host/veld/${s.id}`} className="secondary"><Settings size={14}/> Beheren</Link><Link href={`/veld/${s.slug}`} className="secondary">Bekijken</Link></div></div></div>):<div className="reviewCard"><strong>Nog geen veld aangemeld.</strong><p>Maak je eerste LOS! aan en bepaal zelf prijs en beschikbaarheid.</p><Link href="/host">Start als LOS!-host</Link></div>}<h2 className="accountTitle">Aankomende bezoekers</h2>{hostUpcoming.length?hostUpcoming.map(b=><div className="visitorCard" key={b.id}><div className="visitorIcon"><Users/></div><div><strong>Hondeneigenaar</strong><p><CalendarDays size={13}/> {new Date(b.starts_at).toLocaleString('nl-NL',{dateStyle:'medium',timeStyle:'short'})} · {b.dog_count} hond{b.dog_count>1?'en':''}</p></div><div className="visitorAmount">€{Number(b.total_amount).toFixed(2).replace('.',',')}</div></div>):<div className="reviewCard"><strong>Nog geen aankomende bezoekers.</strong><p>Nieuwe reserveringen verschijnen hier automatisch.</p></div>}</div><aside><div className="sideCard"><h3>Uitbetaling</h3><div className="scoreBig"><Wallet/> €{balance.toFixed(2).replace('.',',')}</div><p>85% van iedere voltooide boeking komt in je saldo. LOS! houdt 15% servicefee in.</p><div className="scoreLine"><span>Bruto</span><strong>€{gross.toFixed(2).replace('.',',')}</strong></div><div className="scoreLine"><span>LOS! fee 15%</span><strong>− €{fee.toFixed(2).replace('.',',')}</strong></div><div className="safeStatus"><CalendarDays/> Maandelijkse uitbetaling</div></div><div className="sideCard"><h3>Beschikbaarheid</h3>{spots.map(s=>{const a=availability.filter(x=>x.spot_id===s.id&&x.enabled);return <div className="availabilityMini" key={s.id}><strong>{s.name}</strong><span>{a.length} dagen per week actief</span></div>})}</div></aside></div></section>}</main>
}